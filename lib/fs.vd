foreign class File {
    construct open(path, mode) {
        _path = path
        _mode = mode
    }

    foreign write(text)
    foreign read()
    foreign close()
}

class Dir {
    foreign static cwd()

    foreign static canonical(path)
}
